// Silver-layer node library: CDC merge, dedup, SCD1/SCD2, DV2.0 hub/link/sat
// builders. Skeleton — populated as templates are added to :jobs.
plugins {
    id("buildsrc.convention.kotlin-jvm")
}

dependencies {
    api(project(":core"))
}

// Gold-layer node library: Kimball dim/fact/aggregate builders and
// ClickHouse/Doris publish nodes. Skeleton — populated as templates are added.
plugins {
    id("buildsrc.convention.kotlin-jvm")
}

dependencies {
    api(project(":core"))
}
